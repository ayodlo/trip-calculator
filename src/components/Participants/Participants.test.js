//Dependencies
import React from 'react';
import { render, cleanup } from '@testing-library/react';
//Components
import TripForm from './TripForm';
//Shared

test('<TripForm />', () => {
    //const wrapper = render(<TripForm />);
    //wrapper.debug();
    //expect(wrapper.getByText('0').tagName).toBe('BUTTON');
  
});
